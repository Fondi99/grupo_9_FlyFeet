<!--  Include de head -->
<%- include("../partials/head.ejs") %>

    <body>
        <%- include("../partials/header.ejs") %>
            <main class="mainLogin">
                <div class="loginImg">
                  <img src="/images/login.png" alt="" />
                </div>
                <div class="loginFormDiv">
                  <h2 class="bienvenida">¡BIENVENIDO DE VUELTA!</h2>
                  <span class="error"><%=locals.errors?.form && `${errors.form.msg}`%></span>
                  <form action="/users/login" method="POST">
                    <div class="loginForm">
                      <label for="email">Email:</label>
                      <input
                        type="text"
                        id="email"
                        name="email"
                        placeholder="Ej.: ejemplo@mail.com"
                        value="<%= typeof old != 'undefined' ? old.email : "" %>"
                      />
                    </div>
                    <div class="loginForm">
                      <label for="password">Contraseña:</label>
                      <input
                        type="password"
                        id="password"
                        name="password"
                        placeholder="Ingrese su contraseña"
                        value="<%= typeof old != 'undefined' ? old.password : "" %>"
                      />
                    </div>
                    <div class="formcheckbox">
                      <div>
                        <input type="checkbox" name="rememberMe" id="remember-me" />
                        <label for="remember-me">Recuérdame</label>
                      </div>
                      <a href="/password_reset" style="text-decoration:none">¿Olvidaste tu contraseña?</a>
                    </div>
                    <% if (typeof errors != 'undefined') { %>
                        <% errors.forEach(error => { %>
                         <ul class="error">
                             <li> <%= error.msg %> </li>
                         </ul>
                        <% }) %>
                       <% } %>
                    <div>
                      <button type="submit" class="form-login">Login</button>
                    </div>
                    <div>
                      <a href="/users/register" style="text-decoration:none" class="form-fork">Creá tu cuenta</a>
                    </div>
                    <br>
                    <br>
                    <div class="meetUs">
                      <a href="/users/contacto">¿QUERÉS CONOCERNOS?</a>
                    </div>
                  </form>
                </div>
              </main>
              <footer class="background">
                <%- include('../partials/footer') %>
              </footer>
    </body>

    </html>